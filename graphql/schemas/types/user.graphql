type User {
        id: ID!
        username: String!
        createdAt: String!
}

type Query {
    user(id: ID!): User
}

input NewUserInput {
    username: String!
    password: String!
}

type Mutation {
    addUser(input: NewUserInput!): User!
}