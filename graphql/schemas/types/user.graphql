type User {
  id: ID!
  username: String!
  createdAt: String!
  snaps: [Snap!]!
}

type Query {
  user(id: ID!): User
  users: [User!]!
}

input NewUserInput {
  username: String!
  password: String!
}

type Mutation {
  addUser(input: NewUserInput!): User!
}
