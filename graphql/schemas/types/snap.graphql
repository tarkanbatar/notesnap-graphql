type Snap {
    id: ID!
    message: String!
    createdAt: String!
    user: User!
}

type Query {
    snap(id: ID!): Snap
    snaps: [Snap!]!
}

input NewSnapInput {
    message: String!
    userId: ID!
}

type Mutation {
    addSnap(input: NewSnapInput!): Snap!
}